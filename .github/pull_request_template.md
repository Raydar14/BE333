<!--
Fill each section in briefly — a short PR that describes itself well is
easier to review than a long one that doesn't. Delete sections that
truly don't apply, but at minimum leave "What changed" and "Why".
-->

## What changed
_One or two sentences describing what this PR does._

## Why
_Why this change is needed — the user problem, the bug, the missing
piece, the config that got flipped, etc. Link an issue if there is one._

## How to test
1. Pull this branch locally: `git checkout <branch-name>`
2. Steps a reviewer follows to verify the change works:
   -
   -

## Screenshots (if the change is visible)
_Before / after screenshots or short screen recordings for UI changes.
Delete this section if the PR only touches non-visual code._

## Checklist
- [ ] I ran the app locally and the change works as described.
- [ ] I reviewed my own diff before requesting review.
- [ ] Any new secrets (API keys, tokens) are in `.env`, never committed.
- [ ] The `TODO.md` is updated if this PR completes or surfaces a task.
